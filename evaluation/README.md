# Retrieval evaluation data

`sec_questions.jsonl` is created locally by the Day 6 CLI. Each line contains one question, optional metadata filters, manually selected relevant chunk IDs, and reviewer notes.

Commit the labeled question dataset after checking that it contains no confidential information. Do not commit generated corpora or AWS credentials.
